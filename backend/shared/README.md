# Shared Packages

Internal libraries shared across services. Imported as Go modules.

- `pkg/tenant` — tenant context helpers
- `pkg/httperr` — uniform HTTP error responses
- `pkg/validator` — input validation helpers
- `pkg/logger` — slog setup
- `pkg/db` — pgx pool helpers
